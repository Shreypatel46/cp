#include<bits/stdc++.h>
#include<math.h>
using namespace std;
int main(){
    int n,min,q;
    cin>>n;
    q=n;
    int size=floor(log10(n))+1;
    if(n/(10*(size-1))!=9){
        while(size){

            }

        }
    }
    else{

    }
}